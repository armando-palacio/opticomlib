name: opticomlib-dev
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -r requirements.txt
      - sphinx
      - renku-sphinx-theme
      - sphinx-rtd-theme
      - sphinx-copybutton
      - pytest
      - build
      - twine
